# Story 3.3: Validator PySpark — Além do ast.parse()

**Status:** ToDo
**Prioridade:** P2
**Esforço:** S
**Responsável:** @dev
**Epic:** Sprint 3 — Transpilation Core
**Origem:** Revisão arquitetural @architect (R4)

## Objetivo
Expandir `transpile/llm/validator.py` para além da verificação de sintaxe Python (`ast.parse()`), adicionando checagens semânticas básicas no código PySpark gerado — detectando problemas comuns antes que o notebook chegue ao Databricks.

## Contexto
`ast.parse()` detecta apenas erros de sintaxe Python. Código PySpark sintaticamente válido mas semanticamente errado (import de `F` ausente, referência a `spark` sem contexto, `saveAsTable` sem Unity Catalog prefix) passa sem aviso. Essas validações adicionais reduzem o número de erros de runtime no Databricks.

## Escopo
### Incluído
- `PySpark Validator` com checagens em cascata:
  1. **Sintaxe Python** — `ast.parse()` (mantém atual)
  2. **Imports necessários** — se código usa `F.col()`, verificar `import pyspark.sql.functions as F`; se usa `Window`, verificar `from pyspark.sql.window import Window`
  3. **Referência a `spark`** — se código usa `spark.read` ou `spark.sql()`, verificar que `spark` é usado (não instanciado — em Databricks é injetado automaticamente)
  4. **Unity Catalog prefix** — se `.saveAsTable("tabela")` sem `catalog.schema.tabela` formato, emitir WARNING (não erro)
  5. **Colunas em strings literais** — detectar padrões como `df["coluna"]` misturado com `F.col("coluna")` e avisar inconsistência de estilo
- `ValidationResult` dataclass:
  ```python
  @dataclass
  class ValidationResult:
      is_valid: bool
      syntax_ok: bool
      warnings: list[ValidationWarning]
      errors: list[ValidationError]
  ```
- Warnings não bloqueiam geração — apenas registrados no relatório de migração

### Excluído
- Validação de schema (verificar se colunas existem) — requer conexão com Databricks
- Execução de código
- Checagem de tipos Spark

## Critérios de Aceitação
- [ ] AC-1: Código sem `import pyspark.sql.functions as F` mas com `F.col()` gera `ValidationError`
- [ ] AC-2: `.saveAsTable("tabela")` sem prefix gera `ValidationWarning` (não erro)
- [ ] AC-3: `ast.parse()` continua como primeira checagem — falha rápida em sintaxe inválida
- [ ] AC-4: `ValidationResult.warnings` e `.errors` aparecem no relatório de migração JSON
- [ ] AC-5: Testes cobrem: código válido, import ausente, saveAsTable sem prefix, Window sem import

## Arquivos Afetados
- `sas2dbx/transpile/llm/validator.py` — expandir validações
- `sas2dbx/models/migration_result.py` — adicionar `ValidationResult`, `ValidationWarning`, `ValidationError`
- `tests/transpile/llm/test_validator.py` — testes unitários

## Definição de Pronto (DoD)
- [ ] Implementação completa
- [ ] Testes passam (`pytest`)
- [ ] Lint passa (`ruff check`)
- [ ] Story atualizada com [x] nos ACs concluídos
