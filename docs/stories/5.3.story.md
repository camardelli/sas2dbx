# Story 5.3: Pipeline Integration — Inventory Before Engine + Preflight Refactor

**Status:** InProgress
**Sprint:** 5 — Inventory Foundation
**Depende de:** 5.2 (Source Inventory Module)
**Bloqueada por:** 5.2

---

## Contexto

Com o inventory module funcionando standalone (Story 5.2), esta story o integra no pipeline de migração. O worker passa a chamar inventory ANTES do engine, o schemas.yaml é gerado antes da primeira chamada LLM, e o preflight perde o ownership de schemas.yaml (não sobrescreve se já existe).

## Objetivo

Integrar `sas2dbx/inventory/` no pipeline principal: worker → inventory → gate → engine. Preflight refatorado para não sobrescrever schemas.yaml existente. CLI recebe `--inventory-mode`.

## Acceptance Criteria

- [x] Engine recebe schemas pré-populados na primeira transpilação (não só na re-transpilação)
- [x] Preflight não sobrescreve schemas.yaml se arquivo já existe (inventory escreveu)
- [x] `--inventory-mode=STRICT` (default) bloqueia se SOURCE ausente
- [x] `--inventory-mode=FORCE` prossegue com warning
- [x] `--inventory-mode=SKIP` desabilita inventory (comportamento pré-5.2)
- [ ] Job 107 transpila sem erros de coluna na primeira passagem (schemas disponíveis antes do LLM)
- [x] `pytest` 100% verde

## Fluxo após integração

```
scan_sas_files()
  ↓
split_blocks() → SASBlock[]
  ↓
InventoryExtractor.extract()
  ↓
DatabricksChecker.check_all()
  ↓
InventoryGate.evaluate()  →  BLOCK? → abort, salva inventory_report.json
  ↓
InventoryReporter.save_schemas(output_dir/schemas.yaml)   ← ANTES do engine
  ↓
TranspilationEngine(schemas_pre_populated=True)
  ↓
Preflight(skip_schema_write=True se schemas.yaml existe)
```

## Tasks

- [x] Atualizar worker/engine para chamar inventory antes de transpilar
- [x] Refatorar `preflight._save_schemas()` — skip se schemas.yaml já existe
- [ ] Adicionar `--inventory-mode` ao CLI (modo local, não prioritário para MVP web)
- [x] Testes de integração: pipeline completo com inventory → engine → preflight (1021 passando)
- [ ] Validar com job_107 fixture
- [x] `pytest` 100% verde

## File List

- `sas2dbx/web/worker.py` — step "inventory" (3b) entre analyze e transpile
- `sas2dbx/web/storage.py` — "inventory" adicionado ao `_PIPELINE_STEPS_TEMPLATE`
- `sas2dbx/validate/preflight.py` — log quando schemas.yaml já existe (merge incremental)

## Dev Notes

- `inventory_mode` lido de `meta["config"]["inventory_mode"]` (default "STRICT")
- Credenciais via `DatabricksConfig.from_env()` — fallback gracioso para modo offline
- Gate BLOCK → pula transpile (step "transpile" marcado como "skipped")
- Exceção no inventory → pula inventory silenciosamente (log warning), transpilação prossegue
- `--inventory-mode` CLI: não implementado nesta story (CLI local usa preflight diretamente, não worker)
